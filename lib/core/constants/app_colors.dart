import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Cartoon Neo-brutalist Pastel Palette
  static const Color background = Color(0xFFFBF8EE); // Soft warm cream canvas
  static const Color backgroundCream = Color(0xFFFFFFFF); // Clean white for modal sheets & cards
  static const Color canvasCream = backgroundCream;
  
  // Ambient Bubble Colors (Matching Signature Home Screen Background)
  static const Color bgSage = Color(0xFFC3E2A0); // Soft sage pastel ambient
  static const Color bgPeach = Color(0xFFE8D4AF); // Warm sandy peach ambient
  static const Color bgLime = Color(0xFFCEF17D); // Soft chartreuse/lime ambient
  static const Color bgSky = Color(0xFFBAE6FD); // Sky blue ambient

  // Playful Cartoon Accents
  static const Color accentLavender = Color(0xFFE0E7FF); // Soft indigo/lavender
  static const Color accentLavenderLight = Color(0xFFEEF2FF);
  static const Color accentPink = Color(0xFFFFD1DC); // Bubblegum rose
  static const Color accentPinkSoft = Color(0xFFFFE4E6);
  static const Color accentYellow = Color(0xFFFEF3C7); // Pastel sunny yellow
  static const Color accentButter = Color(0xFFFFF7CE); // Warm butter yellow
  static const Color accentMint = Color(0xFFD1FAE5); // Fresh cartoon mint
  static const Color accentSky = Color(0xFFE0F2FE); // Soft pastel sky
  static const Color accentLilac = Color(0xFFEDE9FE); // Pastel purple

  // Brand Highlights
  static const Color brandTeal = Color(0xFF00A79D);
  static const Color brandNavy = Color(0xFF0F2444);

  // Cards & Comic Stroke
  static const Color cardWhite = Color(0xFFFFFFFF);
  static const Color strokeBlack = Color(0xFF1E293B); // Comic dark outline
  static const Color textBlack = Color(0xFF0F172A); // Deep comic text
  static const Color textSecondary = Color(0xFF475569); // Secondary text
  static const Color textMuted = Color(0xFF64748B);
  static const Color textPlaceholder = Color(0xFF94A3B8);

  // Buttons & CTAs
  static const Color primary = Color(0xFF0F172A);
  static const Color buttonPrimary = Color(0xFF2563EB); // Vibrant comic blue CTA
  static const Color buttonDark = Color(0xFF0F172A); // Dark pill CTA
  static const Color buttonTextLight = Color(0xFFFFFFFF);

  // Progress & Badges
  static const Color progressBackground = Color(0xFFE2E8F0);
  static const Color progressFilled = Color(0xFF6366F1);

  // Status & Feedback
  static const Color errorRed = Color(0xFFEF4444);
  static const Color successGreen = Color(0xFF10B981);
}
